// ^ for Control
// ~ for Option
// $ for Shift
// # for numeric keypad

{
  "~f" = (moveWordForward:);
  "~b" = (moveWordBackward:);
  "~d" = (deleteWordForward:);
  "~h" = (deleteWordBackward:);
  "^w" = (deleteWordBackward:);
}
