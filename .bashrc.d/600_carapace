# See https://carapace-sh.github.io/carapace-bin/setup/environment.html

case $CURRENT_SHELL in
bash)
    command -v carapace >/dev/null && . <($_ _carapace bash)
    ;;
zsh)
    command -v carapace >/dev/null && . <($_ _carapace zsh)
    ;;
esac
