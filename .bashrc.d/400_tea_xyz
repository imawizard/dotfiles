# Ask for tea's magic to be used.
export TEA_MAGIC=prompt

case $CURRENT_SHELL in
bash)
    test -x "$HOME/.tea/tea.xyz/v*/bin/tea" && . <("$_" --magic=bash --silent)
    ;;
zsh)
    test -x "$HOME/.tea/tea.xyz/v*/bin/tea" && . <("$_" --magic=zsh --silent)
    ;;
esac
